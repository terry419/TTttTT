// --- 파일 위치: Assets/1.Scripts/Core/ICardEffectHandler.cs ---

using UnityEngine;

/// <summary>
/// 모든 카드 효과 처리기(Handler)가 구현해야 하는 인터페이스입니다.
/// 전략 패턴(Strategy Pattern)을 적용하여 각 카드 효과의 실행 로직을 캡슐화합니다.
/// </summary>
public interface ICardEffectHandler
{
    /// <summary>
    /// 카드 효과를 실행합니다.
    /// </summary>
    /// <param name="cardData">실행할 효과가 정의된 카드 데이터입니다.</param>
    /// <param name="executor">PoolManager, PlayerController 등 핵심 참조를 제공하는 EffectExecutor의 인스턴스입니다.</param>
    /// <param name="spawnPoint">효과가 생성될 위치입니다. (예: 몬스터의 위치, 플레이어의 발사 지점)</param>
    void Execute(CardDataSO cardData, EffectExecutor executor, Transform spawnPoint);
}
